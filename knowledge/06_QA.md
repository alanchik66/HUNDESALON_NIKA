# QA

## Minimum checks

- Desktop layout
- Mobile layout
- Locale coverage for `de`, `en`, `ru`, `uk`
- Content correctness
- No duplicate catalog items
- Contact and brand consistency

## Evidence format

- Date
- Surface
- Result
- Screenshot or note
- Follow-up

## Pass criteria

- The fix is visible in the actual UI.
- Locale text matches the selected language.
- No new regression appears in adjacent views.

## 2026-08-30 — Monthly code and artifact cleanup

### Scope and result

- Reviewed the complete first-party source inventory and the staged/unstaged workspace without overwriting unrelated work.
- Removed verified duplicate, obsolete, generated, screenshot, snapshot, and one-off QA artifacts. Recoverable material was sent to the Windows Recycle Bin; build and test output remains reproducible through project scripts.
- Consolidated duplicated price data, CSS overrides, scripts, integrations, configuration, and generated-asset handling while preserving public routes and locale behavior.
- Hardened subscription persistence, consent parsing, uploads, public message drafts, Pages Function headers, calendar free/busy routing, and inbound email forwarding.
- The initial audit did not commit, push, deploy the website, update dependencies, or change DNS/email routing. Its first authorized follow-up updated only the existing info-email Worker's code and forwarding destination; subsequent release work is recorded separately below.

### Verification evidence

- `npm run validate`: 91 HTML files; HTML, CSS, JavaScript, Markdown, links, project configuration, payment guard, email provider, 82 unit tests, and 11 knowledge tests passed.
- `npm run build:production`: 91 HTML files built with one content-derived asset version; contact normalization and deferred/injected scripts completed.
- `npm run check:budgets`: all production CSS, JavaScript, and hero-image budgets passed.
- `npm run price:smoke`: 459/459 checks passed for `de`, `en`, `ru`, and `uk` on desktop and mobile.
- `npm run test:browser`: 16/16 visual checks passed; four WCAG 2.0/2.1 A/AA routes had no serious or critical blockers.
- Manual browser QA: key pages had no horizontal overflow or broken images; all ten breed menus closed on an outside pointer interaction; the Before/After slider changed from 50 to 55 by keyboard; Documents exposed no placeholder links; Telegram WebApp loaded without console warnings.
- Local Pages runtime: canonical and legacy redirects resolved correctly; structured public drafts returned the fixed local template; arbitrary caller-controlled AI messages returned 401; static and Function responses carried the expected security headers.
- `npm run audit:all`: zero reported vulnerabilities in the root project and Google gateway.
- `npm run graphify` and `npm run graphify:check`: the current code graph and report were refreshed and validated after cleanup.

### Staged prompt-file hygiene

- Normalized exactly five staged `.roo/system-prompt-flow-*` files: removed 73 trailing-space occurrences, replaced five meaningful Markdown hard breaks with explicit backslashes, and removed one extra blank line at the debug prompt's EOF.
- All five YAML documents parse successfully; their structure and all 17 affected scalar strings' Markdown rendering are equivalent to the original staged versions.
- Only those five index entries were updated. The other 731 index records, including 37 other staged paths, were unchanged; both staged and unstaged `git diff --check` passed.

### Initial Cloudflare follow-up — verified live on 2026-08-30

- HTTPS `www` requests for `/`, `/ru/prays-list?audit=1`, and `/robots.txt` returned 301 to the apex domain with the same path and query. The existing zone-level redirect was preserved.
- Email Routing is enabled, synced, and ready. The existing `info@hundesalon-nika.com` rule still forwards directly to exactly one verified final inbox; no auto-reply Worker was inserted into the active delivery route.
- Added `INFO_FORWARD_DESTINATION` as an encrypted secret on the existing `info-auto-reply` Worker, using that same verified inbox. No private recipient value was added to source or reports.
- An isolated remote preview using the deployed bindings confirmed that the encrypted forwarding destination matches the live rule and `INFO_AUTOREPLY_SECRET` matches Pages Production. The authenticated `{}` probe returned 400 for missing recipient data; an unauthenticated probe returned 401. No email was sent, and the shared secret was neither displayed nor rotated.
- Deployed only `info-auto-reply`, version `e85f7973-667e-4f93-81fa-e118b3694360`, with forward-first handling and the unused `SEND_EMAIL` binding removed. `workers.dev` and preview URLs remain disabled; the temporary diagnostic sessions were stopped.
- Pages Production remains deployment `79d76b90-8a27-43d6-82be-9a767cc4ff2d`, source `19e5a01`; the website was not redeployed. Both email Workers' unit tests passed (8/8), and the info Worker dry-run passed before deployment.
- This initial checkpoint verified configuration and authorization, not end-to-end mailbox delivery. Auto-reply activation and a controlled delivery test were not yet authorized at that point.

### Authorized release preparation — 2026-08-30

- The user subsequently authorized commit/push, release completion, customer auto-reply activation, and controlled real delivery tests on approved administrative mailboxes. No real customer is used as a test recipient.
- Email regression review found and fixed null-envelope rejection before forwarding, incomplete suppression of automatic/list/report/own-domain senders, raw HTML instead of SendPulse's required UTF-8 Base64 payload, and false success for HTTP 200 provider rejections. Original delivery remains independent of optional reply failure; relay redirects are not followed.
- The named weather-widget container now has the required group role. A deterministic browser assertion covers all four accessibility smoke-test routes.
- The price smoke test now waits for the existing next-frame focus transfer to the selected service before clearing breed search. This preserves all assertions and timeouts and avoids competing with the page's selection-focus operation; production price-page behavior is unchanged.
- Fresh `npm run validate` passed with 117 unit tests and 11 knowledge tests. The final info Worker subset passed 35/35 after the rejection-message wording was corrected; its Wrangler dry-run passed (5.79 KiB, gzip 2.16 KiB).
- Private administrator inbox examples are removed from public setup instructions. Roo home-directory defaults use a runtime-resolved placeholder; canonical project paths and prompt behavior are preserved.
- Release gate: inspect a controlled delivered message before enabling the info routing rule. SendPulse's documented JSON SMTP API exposes neither arbitrary headers nor a raw-MIME field, so a compliant outgoing `Auto-Submitted` header must not be assumed from a successful HTTP response.

### Published release and controlled delivery — 2026-08-30

- Committed and pushed release `92689ff` to `origin/main` (130 reviewed paths). The only excluded local change is `knowledge/.obsidian/workspace.json`; user assets and unfinished local UI state were not deleted. The already-refreshed graph was checked explicitly, so its duplicate post-commit rebuild was skipped for this commit; Git LFS hooks remained active.
- Published Pages Production deployment `9e7e9a31-cb00-42c8-b21b-a55c9adceb72`, source `92689ff`, with asset version `20260830-prod-92689ff-9990c00b`. Published info Worker version `31de67ea-d447-4f4f-8a51-03d1000f29c9` at 100%; both encrypted secret bindings remain configured. No dependencies, DNS records, or unrelated email rules were changed.
- Final local price smoke passed 459/459 after two consecutive RU desktop passes of 73/73. Production `price:smoke:prod` also passed 459/459 with 11 screenshots. Live HTML/canonical/favicon checks passed.
- Live Chrome checks covered DE home, RU price, EN booking, and UK contacts at desktop/mobile sizes: weather group semantics and localized labels were present, with no horizontal overflow or broken loaded images. The in-app browser was unavailable. Three asynchronous messaging errors without a site stack had an undetermined source; an already-open chat panel obscured parts of screenshots and was not changed. Local axe results are not represented as a live axe audit. No customer forms, bookings, chat messages, or payments were submitted.
- Before routing activation, one isolated preview request to the production relay delivered a German branded HTML/plain-text message to the existing verified administrative inbox (Gmail message `1a053844c2824b23`, 16:32:53 UTC). The preview inherited deployed secrets without displaying them and did not publish an HTTP route.
- Updated the existing info rule `dbe22eebbfa24a2680c9ec3904d1827b` from direct forward to `info-auto-reply`, preserving its matcher, enabled state, and priority. The first immediate test was logged as direct-forward delivery at 16:34:12 UTC, without a matching reply. A second distinct test at 16:40:05 UTC produced the automatic German response at 16:40:09 UTC (Gmail message `1a0538af00fa48ec`). Provider acceptance and mailbox delivery were checked separately.
- Correlating the second test by its exact SMTP Message-ID confirmed `eventType=forward`, `status=delivered`, and no error at 16:40:07 UTC. Its separate incoming Worker event is marked `dropped`; that entry alone does not describe the forwarding result. Gmail exposed the original only as its Sent copy, not as a second Inbox item, so the exact-message Cloudflare forwarding event is the delivery evidence for the original.
- Both delivered replies had SPF, DKIM, and DMARC `pass`, valid HTML/plain-text parts, `From` and `Return-Path` at `noreply@hundesalon-nika.com`, and `Reply-To` at `support@hundesalon-nika.com`. Gmail placed them in Inbox/Promotions, not Spam. Tests used only the administrator's own mailbox, never a real customer.
- The actual MIME has no `Auto-Submitted` header. This is a documented provider limitation, not a claim of full RFC 3834 header compliance. [RFC 3834 sections 3.1.7 and 3.3](https://www.rfc-editor.org/rfc/rfc3834.html#section-3.1.7) recommend the header and permit a non-auto-responding return address. Live `noreply@` and `support@` rules each remain direct forwards, so these return paths do not re-enter the info auto-responder. Reassess this protection if those routes change; no unbounded guarantee about arbitrary external responders is implied.
- An expired CLI session exposed an incorrect public OAuth client ID in `tools/lib/cloudflare-auth.mjs` (`invalid_client`, HTTP 401). Matched it to the installed Wrangler client ID, added three isolated mock/temporary-fixture regression tests, and verified a real successful refresh followed by a fresh read of the active info rule. The full unit suite now passes 120/120. No account change or INFO secret replacement was needed.
- The isolated delivery-probe session was stopped after its one authorized send; it did not remain as a deployed endpoint or background listener.
- Final `npm run validate` passed with 120 unit tests and 11 knowledge tests. The post-release follow-up changes only the OAuth maintenance helper, its tests/test command, and documentation/graph evidence; deployed website and email-handler runtime code remain the verified `92689ff` release.

### Post-release code review (local only) — 2026-08-30

- Reviewed the current `c82024b` checkout across form submission, server integrations, local credential-file helpers, and validation commands. This was a targeted correctness review, not a claim that every repository file was rewritten. Existing Obsidian window state was preserved.
- Fixed dev-variable updates/removals that missed whitespace around `=` and left stale or duplicate assignments. Updates now retain one assignment, preserve comments and unrelated keys, and remove process-only values even when the file is absent. Three temporary-fixture tests failed before the fix and pass afterward; real credential files were not changed.
- Booking submission now locks before photo upload and releases through `finally`. Repeated submission during upload or delivery no longer starts another operation; unsuccessful attempts remain retryable. The 4500 ms deadline is restricted to initial local static-server detection, not actual delivery. Automatic fallback POSTs occur only after an explicit local 404/405, never after an unknown network outcome or other HTTP errors.
- `sendmail` and `subscribe` reject JSON null, arrays, and primitives with HTTP 400 before integrations. Error responses preserve allowed-origin CORS. In the SendPulse-unconfigured fallback, a calendar or booking-sheet success cannot substitute for the required client-record write.
- Quoted the globs in `lint:js`, `lint:html`, and `format`. Reproduced the previous POSIX expansion with Git Bash: the JavaScript command received only 16 nested Function files and two Worker test files, omitting core handlers. Quoted ESLint and HTMLHint commands passed under that shell; HTMLHint checked all 91 pages. Formatting was not run across the repository.
- Added 38 regression cases in total: three credential-file cases, 20 server cases, and 15 exact-source frontend cases. Final `npm run validate` passed with 158 unit tests and 11 knowledge tests. Production build passed for 91 HTML pages; five asset budgets passed. `test:browser` passed 16 visual assertions and four accessibility routes with no serious/critical violations. Targeted Prettier, ESLint, syntax, and whitespace checks passed.
- Controlled Chrome UI verification used only a localhost mock server and the English booking form at 1536 × 703. Double-click produced one POST; after 14.457 seconds the request was still single and the button remained disabled. A mock HTTP 500 restored the confirmation button; an explicit retry followed by HTTP 200 displayed success, reset the label, and hid the summary. External sending and Ads requests were blocked; the QA tab and both local test servers were closed.
- Full browser file-upload verification remains limited by the Chrome extension's file-access permission (`Not allowed` from the supported file chooser). The Browser skill's permission boundary was respected; no injection or alternate-browser workaround was used. The pending-upload branch is covered by the exact-source regression tests, not claimed as a completed UI-upload test.
- This pass did not commit, push, deploy, send real messages, or change external settings or secrets. The submission guard does not add server-side idempotency across separate tabs or a manual retry after an unknown network outcome; that stronger guarantee would need its own durable request-identity design.

### Authorized form-safety release preparation — 2026-08-30

- The user subsequently requested completion of commit/push/deploy and the remaining photo check. A fresh fetch confirmed `main` and `origin/main` both at `c82024b`; the live Pages project was still the successful `92689ff` deployment. Existing authenticated access was verified without exposing credentials or changing account configuration.
- Independent final reviews approved the ten changed source/test/documentation paths. The code graph was refreshed and checked explicitly: 3224 nodes, 5039 edges, and 275 communities; its generated report is included as the eleventh release path. Obsidian window state remains excluded.
- A fresh supported Chrome file-chooser attempt still returned `Not allowed`. Requested that the user enable the ChatGPT extension's `Allow access to file URLs`; no permission bypass or external form submission was performed. The earlier automated and controlled local UI results remain the release evidence, not a claim of full photo-upload UI verification.
