Audit multilingual SEO for the scope named after this command (default: recently touched pages).

Use the `seo-auditor` subagent (readonly). Check canonical, hreflang, meta, JSON-LD, Leipzig local relevance across `de|en|ru|uk`. Report findings by severity; do not edit unless the trailing text explicitly asks to fix.
