Review the current diff or named files for over-engineering (Ponytail).

Use the `minimal-diff` subagent (readonly). Find what to delete or simplify — not what to add. Return Delete/simplify, Keep, Verdict.
