Run a Playwright layout smoke for the page or feature named after this command (default: home).

Delegate to the `ui-smoke` subagent when useful. Desktop + phone, `de` plus one other locale. Repair Playwright MCP yourself if down (`npm run mcp:playwright:repair`). Return pass/fail and verdict.
