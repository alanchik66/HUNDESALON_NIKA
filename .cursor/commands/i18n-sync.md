Sync or verify structure/copy consistency across `de|en|ru|uk` for the pages named after this command.

Prefer shared shell/JS/CSS over duplicating markup. Use the `i18n-sync` subagent. Asset paths: language-root `../assets/`, blog `../../assets/`. End with `npm run check:links` if HTML structure changed.
