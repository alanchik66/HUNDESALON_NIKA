Answer using Graphify first for architecture / file relationships.

1. Prefer MCP `graphify` or `npm run graphify:query -- "<question from trailing text>"`.
2. Thin reads only; never dump `graph.json`.
3. Return a short answer with paths and how they connect.
