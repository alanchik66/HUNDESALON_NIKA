/**
 * Disabled: this project keeps only the main branch on GitHub and GitLab.
 */
console.error('GitLab sync MR is disabled. Keep GitHub and GitLab on main only, then use: npm run git:push');
process.exit(1);
