# ═══════════════════════════════════════════════════════════════
#  Prettier ignore — файлы, которые НЕ форматируются автоматически.
#  Держим в синхроне с .gitignore и .stylelintignore.
# ═══════════════════════════════════════════════════════════════

# ─────────── Зависимости и вывод сборки ───────────
node_modules/
dist/
build/
public/
coverage/
.wrangler/
**/.wrangler/

# ─────────── Виджет погоды: вендорный подпроект со своей сборкой ───────────
3d-weather-codrops-main/

# ─────────── Сторонние (вендорные) ассеты — не наш код ───────────
assets/vendor/
assets/fonts/

# ─────────── Минифицированные и сгенерированные файлы ───────────
*.min.js
*.min.mjs
*.min.css

# ─────────── Инструментальные worktree'ы и кеши ───────────
.kilo/
.history/
.playwright-cli/
.playwright-mcp/

# ─────────── Файлы верификации веб-мастеров (должны оставаться байт-в-байт) ───────────
google*.html
BingSiteAuth.xml

# ─────────── Блокировки и служебные данные ───────────
package-lock.json
*.lockb
